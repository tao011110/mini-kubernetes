apiVersion: v1
kind: Pod
metadata:
  name: pod2
  labels:
    name: test

spec:
  containers:
    - name: ghost3
      image: ghost
      command:
      args:
      workingDir:
      volumeMounts:
        - name: volume
          mountPath: /vpath

      ports:
        - name: http1
          containerPort: 320
          hostPort: 320
          protocol: TCP

      resources:
        limits:
          cpu: "1"
          memory: 128M
        requests:
          cpu: 500m
          memory: 1Mi

    - name: nginx3
      image: nginx:latest
      command:
      args:
      workingDir: /home/app/
      volumeMounts:
        - name: volume
          mountPath: /vpath

      ports:
        - name: http2
          containerPort: 100
          hostPort: 100
          protocol: TCP

      resources:
        limits:
          cpu: "1"
          memory: 500M
        requests:
          cpu: "1"
          memory: 10Mi

  volumes:
    - name: config
      hostPath: cpath
